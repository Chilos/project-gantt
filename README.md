# Project Gantt для Logseq

![Version](https://img.shields.io/badge/version-0.1.7-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

Интерактивная диаграмма Ганта для управления проектами в Logseq с полноценным drag-and-drop редактированием.

## ✨ Возможности

### 📊 Управление проектами
- **Проекты** - Организация работы по проектам с названиями и ответственными
- **Этапы разработки** - Настраиваемые этапы с цветами, длительностью и исполнителями
- **Спринты** - Временные отрезки для agile-планирования
- **Мелстоуны** - Важные вехи проекта (релизы, дедлайны)
- **Два режима отображения** - Inline (компактно) или Multiline (каждый этап отдельно)

### 🗓️ Гибкие рабочие дни
- **excludeWeekdays** - Исключение дней недели (0=воскресенье, 6=суббота)
- **includeDates** - Включение конкретных дат как рабочих (например, субботы)
- **excludeDates** - Исключение конкретных дат (праздники, выходные)
- **weekStartsOn** - День начала недели: 0=воскресенье, 1=понедельник (по умолчанию понедельник)
- **Приоритеты**: excludeDates > includeDates > excludeWeekdays

### 🎮 Интерактивность
- **Drag & Drop** - Перетаскивание этапов и мелстоунов по временной шкале
- **Resize этапов** - Изменение длительности через правый хендл
- **Границы временной шкалы** - Элементы не выходят за пределы дат
- **Визуальная индикация** - Красная рамка при достижении границ
- **Автосохранение** - Все изменения автоматически сохраняются в Logseq

### 🎨 Визуализация
- **Два режима масштаба времени** - По дням или по неделям (`timeScale: 'day'` или `'week'`)
- **Дни недели** - Отображение на русском (пн, вт, ср, чт, пт, сб, вс)
- **Недели** - Отображение в формате "28-3" (с 28 числа по 3 число), поддержка локализации
- **Поддержка локалей** - Неделя может начинаться с понедельника или воскресенья
- **Цветные этапы** - 10 адаптивных цветов с автоматическим контрастом текста
- **Подсветка текущего дня** - Всегда видно где вы находитесь (только в режиме дней)
- **Количество дней/недель** - Отображается на каждом этапе (в режиме недель показывает недели)
- **Тёмная и светлая темы** - Автоматическая адаптация к теме Logseq

## 📦 Установка

### Из исходного кода

```bash
git clone <repository-url>
cd project-gantt
npm install
npm run build
```

Затем в Logseq:
1. Откройте `Settings → Plugins → Load unpacked plugin`
2. Выберите папку `progect-gantt`

## 🚀 Использование

### Создание диаграммы

Используйте slash команду `/Project Gantt` в любой заметке Logseq. Будет вставлен макрос:

```
{{renderer project-gantt, <base64-data>}}
```

### Базовая структура данных

После создания диаграммы вы увидите пустую временную шкалу. Данные хранятся в base64-кодированном JSON внутри блока.

### Режимы масштаба времени

Плагин поддерживает два режима отображения времени:

**Режим дней (по умолчанию):**
- Каждая ячейка = 1 рабочий день
- Отображается текущий день
- Учитываются рабочие дни (excludeWeekdays, includeDates, excludeDates)

**Режим недель:**
```json
{
  "timeScale": "week",
  "weekStartsOn": 1
}
```
- Каждая ячейка = 1 неделя (по умолчанию понедельник - воскресенье)
- Ширина ячейки в 2 раза больше
- Линия текущего дня не отображается
- Спринты указываются в неделях
- `weekStartsOn: 0` для недель воскресенье-суббота (США, Канада)
- `weekStartsOn: 1` для недель понедельник-воскресенье (Европа, Россия)

### Пример данных (внутреннее представление)

```json
{
  "startDate": "2024-01-01",
  "endDate": "2024-01-31",
  "excludeWeekdays": [0, 6],
  "projects": [
    {
      "id": "proj1",
      "name": "Веб-приложение",
      "layout": "inline",
      "stages": [
        {
          "id": "stage1",
          "name": "Анализ",
          "start": "2024-01-01",
          "duration": 5,
          "assignee": { "name": "Иванов И.И." },
          "color": "#FF6B6B"
        },
        {
          "id": "stage2",
          "name": "Разработка",
          "start": "2024-01-08",
          "duration": 10,
          "assignee": { "name": "Петров П.П." },
          "color": "#4ECDC4"
        }
      ],
      "milestones": [
        {
          "id": "mile1",
          "name": "ПСИ",
          "date": "2024-01-20",
          "assignee": { "name": "Руководитель" },
          "color": "#FFD93D"
        }
      ]
    }
  ],
  "sprints": [
    {
      "id": "sprint1",
      "name": "Спринт 1",
      "start": "2024-01-01",
      "end": "2024-01-14"
    }
  ]
}
```

### Интерактивное редактирование

Плагин предлагает два способа редактирования диаграмм Ганта:

#### 1. Модальный редактор (рекомендуется)

Нажмите кнопку **"Редактировать"** над диаграммой, чтобы открыть полнофункциональный редактор.

**Возможности редактора:**

- **Вкладка "Проекты"**:
  - Создание, редактирование и удаление проектов
  - Добавление/редактирование этапов (название, дата, длительность, исполнитель, цвет)
  - Добавление/редактирование мелстоунов (название, дата, исполнитель, цвет)
  - Выбор режима отображения (Inline/Multiline)

- **Вкладка "Спринты"**:
  - Создание, редактирование и удаление спринтов
  - Настройка дат начала и окончания

- **Вкладка "Настройки"**:
  - Изменение диапазона дат диаграммы
  - Выбор дня начала недели (понедельник/воскресенье)
  - Выбор масштаба времени (день/неделя)
  - Исключение дней недели (выходные)
  - Добавление рабочих дат (переопределение выходных)
  - Исключение конкретных дат (праздники)

После внесения изменений нажмите **"Сохранить"**, и страница автоматически перезагрузится с обновленными данными.

#### 2. Drag & Drop редактирование

Быстрое изменение дат и длительности прямо на диаграмме:

**Перемещение этапов:**
1. Наведите курсор на этап
2. Нажмите и удерживайте левую кнопку мыши
3. Перетащите этап на новую позицию
4. Отпустите - изменения сохранятся автоматически

**Изменение длительности этапа:**
1. Наведите на правый край этапа
2. Курсор изменится на `↔`
3. Нажмите и тяните вправо/влево
4. Отпустите - новая длительность сохранится

**Перемещение мелстоунов:**
1. Кликните на ромб (мелстоун)
2. Перетащите на нужную дату
3. Отпустите - дата обновится

### Настройка рабочих дней

Исключите выходные и добавьте праздники:

```json
{
  "excludeWeekdays": [0, 6],
  "includeDates": ["2024-01-06"],
  "excludeDates": ["2024-01-01", "2024-01-07"]
}
```

**Логика работы:**
- `excludeDates` имеет наивысший приоритет (дата будет исключена несмотря ни на что)
- `includeDates` переопределяет `excludeWeekdays`
- `excludeWeekdays` исключает дни недели (0=вс, 1=пн, ..., 6=сб)

### Режимы отображения проектов

**Inline режим (по умолчанию):**
Все этапы на одной строке
```json
{
  "layout": "inline"
}
```

**Multiline режим:**
Каждый этап на отдельной строке (удобно для сложных проектов)
```json
{
  "layout": "multiline"
}
```

## 🎨 Цветовая схема

Плагин автоматически генерирует 10 адаптивных цветов на основе акцентного цвета вашей темы Logseq. Цвет текста автоматически выбирается для максимального контраста.

Рекомендуемые цвета для этапов:
- **Анализ**: `#FF6B6B` (красно-оранжевый)
- **Дизайн**: `#F39C12` (оранжевый)
- **Разработка**: `#4ECDC4` (бирюзовый)
- **Тестирование**: `#45B7D1` (голубой)
- **Деплой**: `#96CEB4` (зеленый)

Для мелстоунов:
- **ПСИ**: `#FFD93D` (золотой)
- **Релиз**: `#6C5CE7` (индиго)
- **Деплой**: `#E17055` (красно-оранжевый)

## 🛠️ Разработка

### Структура проекта

```
src/
├── core/           # Бизнес-логика (GanttManager)
├── storage/        # Работа с Logseq блоками (GanttDataManager)
├── ui/             # UI компоненты (GanttRenderer, VisualEditor)
├── utils/          # Утилиты (dateUtils, colorSystem, encoding, positionUtils)
├── styles/         # CSS стили
├── types/          # TypeScript типы
├── plugin.ts       # Главный класс плагина
└── index.ts        # Точка входа
```

### Команды

```bash
# Разработка с hot reload
npm run dev

# Сборка production
npm run build

# Релизная сборка (минифицированная)
npm run build:release

# Запуск тестов
npm test

# Тесты с UI
npm run test:ui

# Покрытие кода
npm run test:coverage
```

### Добавление нового свойства этапа

1. Обновите тип `Stage` в `src/types/index.ts`
2. Обновите `SerializableStage` там же
3. Обновите `toSerializable`/`fromSerializable` в `src/utils/encoding.ts`
4. Добавьте рендеринг в `src/ui/GanttRenderer.ts`

### Технологии

- **TypeScript** - Строгая типизация
- **Logseq Plugin API** - Интеграция с Logseq
- **esbuild** - Быстрая сборка
- **Vitest** - Тестирование
- **CSS Variables** - Адаптация к темам

## 📝 Примеры использования

### Планирование спринта (режим дней)

Создайте диаграмму с двумя спринтами и отследите прогресс:

```json
{
  "startDate": "2024-02-01",
  "endDate": "2024-02-29",
  "excludeWeekdays": [0, 6],
  "sprints": [
    { "name": "Спринт 5", "start": "2024-02-01", "end": "2024-02-14" },
    { "name": "Спринт 6", "start": "2024-02-15", "end": "2024-02-28" }
  ],
  "projects": [
    {
      "name": "Новая функциональность",
      "stages": [
        { "name": "Исследование", "start": "2024-02-01", "duration": 3 },
        { "name": "Разработка", "start": "2024-02-06", "duration": 8 },
        { "name": "Тестирование", "start": "2024-02-16", "duration": 5 }
      ],
      "milestones": [
        { "name": "Демо", "date": "2024-02-26" }
      ]
    }
  ]
}
```

### Долгосрочное планирование (режим недель)

Для больших проектов используйте недельный режим:

```json
{
  "startDate": "2024-01-01",
  "endDate": "2024-06-30",
  "timeScale": "week",
  "sprints": [
    { "name": "Q1", "start": "2024-01-01", "end": "2024-03-31" },
    { "name": "Q2", "start": "2024-04-01", "end": "2024-06-30" }
  ],
  "projects": [
    {
      "name": "Веб-платформа",
      "stages": [
        { "name": "Фаза 1", "start": "2024-01-01", "duration": 8 },
        { "name": "Фаза 2", "start": "2024-03-01", "duration": 12 },
        { "name": "Запуск", "start": "2024-05-27", "duration": 4 }
      ],
      "milestones": [
        { "name": "Альфа", "date": "2024-02-26" },
        { "name": "Бета", "date": "2024-05-20" }
      ]
    }
  ]
}
```

## 🐛 Известные ограничения

- Данные хранятся в base64, прямое редактирование JSON в блоке неудобно (используйте модальный редактор)
- Нет контекстного меню (правый клик) на элементах диаграммы
- Нет горячих клавиш для навигации

## 🗺️ Roadmap

- [x] Модальный редактор с формами для добавления элементов ✅
- [ ] Контекстное меню (правый клик) для быстрого редактирования/удаления
- [ ] Горячие клавиши (стрелки для перемещения, Delete для удаления)
- [ ] История изменений (undo/redo)
- [ ] Экспорт в PNG/PDF
- [ ] Зависимости между задачами (стрелки связей)
- [ ] Процент выполнения этапов
- [ ] Критический путь
- [ ] Импорт из других форматов (CSV, MS Project)

## 📄 Лицензия

MIT License

## 🤝 Вклад в проект

Issues и Pull Requests приветствуются!

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь что данные в блоке валидны
3. Создайте issue с описанием проблемы

---

Made with ❤️ for Logseq community
